apiVersion: v1
kind: Service
metadata:
  name: tessera-backend-service
spec:
  selector:
    app: tessera-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
